// import { cn } from '@/UI/utils/cn';

// interface BadgeProps
// {
//     variant?: 'default' | 'outline' | 'destructive' | 'outline-destructive';
//     size?: 'default' | 'lg';
// }

// const Badge = ( { variant = 'default', size = 'default' }: BadgeProps ) =>
// {
//     return (
//         <span
//             className={ cn(
//                 'inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-medium',
//                 variant === 'default' && 'bg-primary text-primary-foreground',
//                 variant === 'outline' && 'border border-primary text-primary',
//                 variant === 'destructive' && 'bg-destructive text-destructive-foreground',
//                 variant === 'outline-destructive' && 'border border-destructive text-destructive',
//                 size === 'default' && 'px-2.5 py-0.5',
//                 size === 'lg' && 'px-3 py-1'
//             ) }
//         >
//             Badge
//         </span>
//     );
// };

// export default Badge;
